#!/usr/bin/env node
var path    = require('path');
var fixpack = require('../fixpack');
var args    = process.argv.slice(2);
var files   = (args.length !== 0) ? args : ['package.json', 'bower.json'];

files.forEach(function(file){
	fixpack(path.resolve(file), true);
});
